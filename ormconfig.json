{
    "type": "postgres",
    "host": "localhost",
    "port": 8500,
    "username": "postgres",
    "password": "postgres",
    "database": "postgres",
    "entities": ["dist/**/*.entity{.ts,.js}"],
    "synchronize": true,
    "migrations": [
        "dist/migration/*.{ts,js}"
    ],
    "cli": {
        "migrationsDir": "migration"
    }
}

